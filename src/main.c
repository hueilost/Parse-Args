#include <stdio.h>
#include "../include/parse_cpp.h"


int main(int argc, char *argv[]){
    struct ParseC *parse; 
    
 	char arg[100];

    gets(arg);
    scanf("%c", arg);
    printf("pointer");

    return 0;
}
